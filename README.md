## Minimal WebRTC demo

Requirements:
node.js (at least version 5)

Install: `npm install`

Run: `node server.js`

The goto this address in a browser (Chrome v51+, Firefox v47+):
http://localhost:8739/
